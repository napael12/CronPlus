Application will maintain storage of variables, which can be used in Workflow/Step configurations
using {variable1} expression.  Variables will be stored in settings database

variable expressions can be snippets of python code, which will be evaluated before running step or workflow
