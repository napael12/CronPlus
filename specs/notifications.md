provide notification using:

a. SMTP mail.  
    STMP mail notification should be stored as {notification.mailhost | port } settings
    {subject.template} = "CronPlus {instance} - {workflow}: {status}"
    {notificaiton.sender}
